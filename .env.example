# --- GENERAL PIPELINE SETTINGS ---
NUM_POSTS_TO_COLLECT=100
SLEEP_TIMER=3.0

# --- ARXIV SETTINGS ---
ARXIV_DELAY=3.0
ARXIV_MAX_RESULTS_PER_REQUEST=100

# --- SEMANTIC SCHOLAR SETTINGS ---
SEMANTIC_SCHOLAR_DELAY=1.0
SEMANTIC_SCHOLAR_MAX_RESULTS_PER_REQUEST=100
# Optional: Get API key from https://www.semanticscholar.org/product/api for higher rate limits
SEMANTIC_SCHOLAR_API_KEY=""

# --- DIRECTORY SETTINGS ---
ORIGINAL_ONLY_DIR="corpora/original_only"

# --- DEFAULT SEARCH SETTINGS ---
DEFAULT_ARXIV_QUERY="machine learning"
DEFAULT_ARXIV_CATEGORY=""

# --- SEMANTIC SCHOLAR DEFAULT SETTINGS ---
DEFAULT_SEMANTIC_SCHOLAR_FIELD="Psychology"
DEFAULT_SEMANTIC_SCHOLAR_QUERY="research"

# --- API KEYS ---
# Only needed if using the separate LLM corpus generator (scripts/generate_llm_corpus.py)
GEMINI_API_KEY=""
