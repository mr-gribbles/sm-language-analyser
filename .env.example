# --- GENERAL PIPELINE SETTINGS ---
NUM_POSTS_TO_COLLECT=100
SLEEP_TIMER=3.0

# --- ARXIV SETTINGS ---
ARXIV_DELAY=3.0
ARXIV_MAX_RESULTS_PER_REQUEST=100

# --- LLM REWRITER SETTINGS ---
LLM_MODEL="gemini-2.5-flash"
REWRITE_PROMPT_TEMPLATE="Please rewrite the following academic text in a different style while maintaining the technical accuracy and meaning. Do not add any commentary, just provide the rewritten text:\n\n---\n\n{text_to_rewrite}"

# --- DIRECTORY SETTINGS ---
ORIGINAL_ONLY_DIR="corpora/original_only"
REWRITTEN_PAIRS_DIR="corpora/rewritten_pairs"

# --- DEFAULT SEARCH SETTINGS ---
DEFAULT_ARXIV_QUERY="machine learning"
DEFAULT_ARXIV_CATEGORY=""

# --- API KEYS ---
# Only needed if using LLM rewriting functionality
GEMINI_API_KEY=""
